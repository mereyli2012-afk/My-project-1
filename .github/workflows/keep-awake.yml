name: Keep Supabase Awake

on:
  schedule:
    - cron: "17 5 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase health endpoint
        run: |
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" \
            -H "apikey: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImpkcHNsZmpoenhiZ3h6dHB6bWFnIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODI3ODg2NTEsImV4cCI6MjA5ODM2NDY1MX0.QdvhlBLgGUFwK3xhQ1YVgyzjbLMfLVCd536iyJMMqzI" \
            "https://jdpslfjhzxbgxztpzmag.supabase.co/auth/v1/health")
          echo "HTTP code: ${HTTP_CODE}"
